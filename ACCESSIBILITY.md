# ACCESSIBILITY

- Mobile-first layout with large touch targets (min 44px controls)
- Keyboard focus visibility on all interactive controls
- Color-independent textual statuses via JSON state output
- Reduced-motion support through `prefers-reduced-motion`
- Text mode always available when permissions fail
